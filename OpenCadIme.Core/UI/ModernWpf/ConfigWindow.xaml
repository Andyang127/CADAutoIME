<Window x:Class="OpenCadIme.UI.ModernWpf.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CAD Auto IME 白名单配置"
        Height="720" Width="640"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI, Microsoft YaHei" FontSize="13"
        MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}">

    <Window.Resources>
        <SolidColorBrush x:Key="WindowBg" Color="#333333"/>
        <SolidColorBrush x:Key="CardBg" Color="#424242"/>
        <SolidColorBrush x:Key="TitleBarBg" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="AccentColor" Color="#007ACC"/>
        <SolidColorBrush x:Key="AccentHover" Color="#0098FF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#B4B4B4"/>
        <SolidColorBrush x:Key="BorderLine" Color="#555555"/>
        <SolidColorBrush x:Key="ButtonBg" Color="#555555"/>
        <SolidColorBrush x:Key="ButtonHover" Color="#666666"/>
        <SolidColorBrush x:Key="InputBg" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="ThumbNormalBg" Color="#5A6578"/>
        <SolidColorBrush x:Key="ThumbHoverBg" Color="#8A99B0"/>

        <DropShadowEffect x:Key="WindowShadow" BlurRadius="16" ShadowDepth="2" Opacity="0.3" Color="Black"/>
        <DropShadowEffect x:Key="ButtonShadow" BlurRadius="6" ShadowDepth="2" Opacity="0.25" Color="Black"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBg}"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="14,5,14,5"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLine}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentColor}"/>
        </Style>

        <Style x:Key="ToolButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLine}"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DarkScrollThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="thumbBorder" Background="{DynamicResource ThumbNormalBg}" CornerRadius="2.5" Margin="0" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource ThumbHoverBg}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource AccentColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkVerticalScrollBar" TargetType="ScrollBar">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Width" Value="5" />
            <Setter Property="MinWidth" Value="5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate><Border Background="Transparent" /></ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate><Border Background="Transparent" /></ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource DarkScrollThumb}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0" CanContentScroll="{TemplateBinding CanContentScroll}" />
                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1"
                                       Style="{StaticResource DarkVerticalScrollBar}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 外框：统一 CadAutoSnap 8px 圆角 + 16px 暗黑投影 -->
    <Border Background="{DynamicResource WindowBg}"
            CornerRadius="8"
            BorderBrush="{DynamicResource BorderLine}"
            BorderThickness="1"
            Margin="12"
            Effect="{DynamicResource WindowShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 1. 顶部标准标题栏 (CadAutoSnap 规范：TitleBarBg + 拖拽 + 8px圆角 + 红底悬停关闭按钮) -->
            <Border Grid.Row="0" Background="{DynamicResource TitleBarBg}" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                        <Image x:Name="imgLogo" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock x:Name="lblTitle" Text="CAD Auto IME 白名单配置" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" />
                        <TextBlock x:Name="lblVersion" Text="v0.4.3" Foreground="#00E5FF" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center" Opacity="0.85" />
                        <TextBlock x:Name="lblCheckUpdate" Text="[发现新版，点击更新]" FontSize="11" Foreground="#28BE50" Margin="10,0,0,0" VerticalAlignment="Center" Cursor="Hand" Visibility="Collapsed" MouseLeftButtonDown="LblCheckUpdate_MouseLeftButtonDown" />
                    </StackPanel>

                    <Button HorizontalAlignment="Right" Width="44" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextSecondary}" FontSize="15" Content="✕" Click="BtnClose_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bg" Background="Transparent" CornerRadius="0,8,0,0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background" Value="#E81123"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- 2. 中间内容区 -->
            <Grid Grid.Row="1" Margin="0,8,0,4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="6*" />
                    <RowDefinition Height="4*" />
                </Grid.RowDefinitions>

                <!-- 搜索过滤条 -->
                <Border Grid.Row="0" Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderLine}" BorderThickness="1" CornerRadius="6" Margin="14,4,14,6" Height="36" Padding="10,0">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔍 " VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}" FontSize="12" />
                        <Grid Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="txtSearchWatermark" 
                                       Text="此处可以搜索并定位指令" 
                                       Foreground="#777777" 
                                       FontSize="13" 
                                       VerticalAlignment="Center" 
                                       IsHitTestVisible="False" 
                                       Margin="2,0,0,0"/>
                            <TextBox x:Name="txtSearch" 
                                     Background="Transparent" 
                                     Foreground="{DynamicResource TextPrimary}" 
                                     BorderThickness="0" 
                                     VerticalAlignment="Center" 
                                     FontSize="13" 
                                     TextChanged="TxtSearch_TextChanged" />
                        </Grid>
                    </Grid>
                </Border>

                <!-- 卡片 1：我的扩展指令 -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <TextBlock Text="✦ 我的扩展指令 (须输入命令全称，而非快捷键)" Style="{StaticResource HeaderStyle}" />
                        </Grid>

                        <Grid Grid.Row="1" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="76" />
                            </Grid.ColumnDefinitions>
                            <Border Background="{DynamicResource InputBg}" BorderBrush="{DynamicResource BorderLine}" BorderThickness="1" CornerRadius="4" Margin="0,0,8,0">
                                <TextBox x:Name="txtNewCmd" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" Padding="8,4" VerticalContentAlignment="Center" FontSize="13" KeyDown="TxtNewCmd_KeyDown" />
                            </Border>
                            <Button Grid.Column="1" Content="添加" Style="{StaticResource ToolButtonStyle}" Background="{DynamicResource AccentColor}" BorderBrush="{DynamicResource AccentColor}" Foreground="White" FontWeight="SemiBold" Height="30" Click="BtnAdd_Click" />
                        </Grid>

                        <ScrollViewer Grid.Row="2" Style="{StaticResource DarkScrollViewer}" VerticalScrollBarVisibility="Auto">
                            <WrapPanel x:Name="wpCustom" />
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- 卡片 2：系统内置核心指令 -->
                <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <TextBlock Text="✦ 系统内置指令" Style="{StaticResource HeaderStyle}" HorizontalAlignment="Left" />
                            <TextBlock Text="⚠ 建议不要删除核心指令" Foreground="#FF6B6B" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        </Grid>

                        <ScrollViewer Grid.Row="1" Style="{StaticResource DarkScrollViewer}" VerticalScrollBarVisibility="Auto">
                            <WrapPanel x:Name="wpCore" />
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- 3. 底部控制与操作栏 -->
            <Border Grid.Row="2" Background="{DynamicResource TitleBarBg}" CornerRadius="0,0,8,8" BorderBrush="{DynamicResource BorderLine}" BorderThickness="0,1,0,0" Padding="14,10">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="接管模式：" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" FontSize="12" />
                                <RadioButton x:Name="RbProcess" Content="进程控制 (推荐)" IsChecked="True" Cursor="Hand" Margin="4,0,12,0" ToolTip="仅在 CAD 窗口激活时切换，切出到微信/浏览器保持原样。适合微信输入法等现代框架。" />
                                <RadioButton x:Name="RbGlobal" Content="全局控制" Cursor="Hand" ToolTip="强制接管 Windows 系统全局键盘布局。适合老旧输入法或经常状态错乱的情况。" />
                            </StackPanel>
                            <TextBlock Text="快捷键: 双击删除指令 | Ctrl+Z 撤销 | Ctrl+Y 重做" Foreground="{DynamicResource TextSecondary}" FontSize="11" Opacity="0.8" />
                            <TextBlock x:Name="lblStatus" Text="✅ 状态提示" Foreground="#00E5FF" FontSize="11" Margin="0,2,0,0" Visibility="Collapsed" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="恢复默认" Width="88" Height="32" Style="{StaticResource ToolButtonStyle}" Margin="0,0,10,0" Click="BtnReset_Click" />
                            <Button x:Name="btnSave" Content="保存应用" Width="110" Height="32" Style="{StaticResource ToolButtonStyle}" BorderThickness="0" Foreground="White" FontWeight="Bold" Click="BtnSave_Click">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#007ACC" Offset="0.0" />
                                        <GradientStop Color="#005A9E" Offset="1.0" />
                                    </LinearGradientBrush>
                                </Button.Background>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,10,0,2">
                        <TextBlock HorizontalAlignment="Left" FontSize="11" Opacity="0.7">
                            <Hyperlink NavigateUri="https://github.com/Andyang127/CadAutoIme" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{DynamicResource AccentColor}" TextDecorations="None">💖 开源地址</Hyperlink>
                        </TextBlock>
                        <TextBlock HorizontalAlignment="Right" FontSize="11" Opacity="0.6">
                            <Run Text="Developed by " Foreground="{DynamicResource TextSecondary}"/>
                            <Hyperlink NavigateUri="https://v.douyin.com/EQGXP6k0l2g/" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{DynamicResource AccentColor}" TextDecorations="None">浅醉·墨语</Hyperlink>
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>